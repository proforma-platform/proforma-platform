/** @type {import('next').NextConfig} */
const nextConfig = {
  generateBuildId: async () => "gov-manager-v7-baseline",
  poweredByHeader: false,
  reactStrictMode: true
};

export default nextConfig;
